- clean: ['~']

- link:
    ~/.tmux.conf: .tmux.conf
    ~/.zshrc: .zshrc.rpi
    ~/.config/kak/autoload:
        create: true
        path: .config/kak/autoload

- shell:
    - [git submodule update --init --recursive, Installing submodules]
